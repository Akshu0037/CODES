#include<stdio.h>
#include<conio.h>

int main()
{
	int a;
	printf("Enter your age :");
	scanf("%d",&a);
	if(a>=18)
	{
		printf("You can vote."); //executes if condition is true.
	}
	else
	{
		printf("You can not vote."); //executes if the condition is false.
	}
}
